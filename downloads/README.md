# Downloads folder

This folder is only a local fallback for previewing the site outside GitHub Pages.

For public distribution, upload these files to GitHub Releases instead of committing them here:

- `ArnMeshSetup.exe`
- `ArnMesh-Android-release.apk`

The site automatically points to GitHub Release assets when it is hosted at `https://OWNER.github.io/REPO/`.
